#Name of this application
spring.application.name: quaza-docker-sample

#URL of the Spring enabled Cloud Config Server
spring.cloud.config.uri: http://localhost:8888

# Disable security since all Actuator endpoints are secured by default
management.security.enabled=false